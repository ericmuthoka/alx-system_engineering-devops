sudo ufw default deny incoming  # Deny all incoming traffic by default
sudo ufw allow 22/tcp  # Allow incoming traffic on port 22 (SSH)
sudo ufw allow 443/tcp  # Allow incoming traffic on port 443 (HTTPS SSL)
sudo ufw allow 80/tcp  # Allow incoming traffic on port 80 (HTTP)
sudo ufw enable  # Enable ufw with the configured rules
